<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="#0069C0"
    android:padding="16dp">

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Platos"
        android:textColor="#FFFFFF"
        android:textSize="24sp"
        android:textStyle="bold"
        android:layout_marginTop="24dp"
        android:layout_marginBottom="8dp"/>

    <!-- Indicación al usuario -->
    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Ingrese un ingrediente principal para buscar platos disponibles."
        android:textColor="#BBDEFB"
        android:textSize="13sp"
        android:layout_marginBottom="4dp"/>

    <!-- Ejemplo -->
    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Ej: chicken_breast, salmon, beef"
        android:textColor="#BBDEFB"
        android:textSize="12sp"
        android:textStyle="italic"
        android:layout_marginBottom="12dp"/>

    <EditText
        android:id="@+id/etIngrediente"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:hint="Buscar por ingrediente..."
        android:textColorHint="#BBDEFB"
        android:textColor="#FFFFFF"
        android:backgroundTint="#FFFFFF"
        android:layout_marginBottom="8dp"/>

    <Button
        android:id="@+id/btnBuscar"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Buscar"
        android:textColor="#0069C0"
        android:backgroundTint="#FFFFFF"
        android:layout_marginBottom="16dp"/>

    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/rvMeals"
        android:layout_width="match_parent"
        android:layout_height="match_parent"/>

</LinearLayout>